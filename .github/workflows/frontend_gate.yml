name: frontend_gate

on:
  pull_request:
    paths:
      - 'frontend/**'
      - '.github/workflows/frontend_gate.yml'
  push:
    branches: [main]
    paths:
      - 'frontend/**'
      - '.github/workflows/frontend_gate.yml'

jobs:
  flutter:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend

    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
      - run: flutter pub get
      - name: Flutter analyze baseline guard
        run: python3 tool/check_analyze_baseline.py --baseline-file tool/analyze_baseline.txt
      - name: Flutter tests
        run: flutter test
